# v0.5 PR Spec (2026-03-01)

## 0. Baseline

v0.5 仅处理以下两类内容：

1. rulings/modules 中明确标注 `v0.5+` 或未在 v0.4 闭合的项；
2. DI-0~DI-5 中已记录但未进入 v0.3/v0.4 的开放设计项。

## 1. Objectives

1. 完成 R12/R13/R14 的高级能力收口。
2. 形成多编辑范式下可证明的一致性与恢复模型。
3. 通过性能与回归门禁，将 v0.4 引入的新复杂度压回可控区间。

## 2. Scope Definition

### Must Have

- R12：P2 元素（`atom_embed/group`）与 Spatial/Canvas 深度联动
- R12：统一 block tree 是否采用的结论与落地路线（评估后执行）
- R13：4 个开放设计项闭合（实现或有约束延期）
- R14：reconciliation 强化（匹配、orphan、超时与回退）
- DI-4：LRU(N) 渲染缓存 + 资源生命周期策略落地
- DI-5：Undo/Redo 基础能力落地
- S6：多 provider 冲突体验统一

### Conditional

- 若 v0.4 未完成 S5 bridge，则 v0.5 升级为 Must Have

## 3. PR Sequence

| 顺序 | PR | 目标 | 关键交付 | 依赖 |
|---|---|---|---|---|
| 1 | `PR-0501` | R12 P2 | `atom_embed/group`、Canvas/Workspace 嵌入关系与索引 | v0.4 完成 |
| 2 | `PR-0502` | R12 统一模型决策 | block tree 评估报告 + 选定路线 + 实施首版 | PR-0501 |
| 3 | `PR-0503` | R13 高级会话能力 | 上下文引用、AI 生成 Atom、长对话增长策略、扩展集成基础 | v0.4 Conversation MVP |
| 4 | `PR-0504` | R14 强化 | reconciliation 算法、orphan UX、失败回退一致性 | v0.4 overlay 基础 |
| 5 | `PR-0505` | S6 高级同步 | 多 provider 冲突策略与交互流程统一 | v0.4 sync runtime |
| 6 | `PR-0506` | DI-4 性能收口 | LRU(N) 渲染缓存、内容加载/驱逐策略、性能门禁 | PR-0501, PR-0504 |
| 7 | `PR-0507` | DI-5 开放项 | Undo/Redo 基础协议与跨 pane 行为定义 | PR-0506 |
| 8 | `PR-0508` | 持久化升级 | layout schema v2、view mode/恢复策略升级、兼容迁移 | PR-0506, PR-0507 |

## 4. Gate Design

### Gate A: Model Convergence

在 `PR-0504` 后：

- block/content/overlay 三层关系无歧义
- 文本与 block 模式切换不发生静默数据丢失

### Gate B: Runtime Stability

在 `PR-0506` 后：

- 多 pane 长会话下无明显卡顿回归
- LRU 策略下切换延迟与内存占用在目标阈值内

### Gate C: Workflow Completeness

在 `PR-0508` 后：

- Undo/Redo、恢复、冲突处理三条链路可联合验收
- schema 升级可回退、可恢复、可诊断

## 5. Coverage Matrix

### Rulings Coverage

| Ruling | v0.5 对应 PR |
|---|---|
| S1 R12（P2 + 统一模型） | PR-0501, PR-0502 |
| S1 R13（开放项闭合） | PR-0503 |
| S1 R14（强化） | PR-0504 |
| S5（若未完成） | PR-0505 或单独补丁 PR |
| S6（多 provider/冲突） | PR-0505 |

### Module Coverage

| Module | v0.5 对应 PR |
|---|---|
| `core-editor/edit-buffer` 高级策略 | PR-0506, PR-0507 |
| `core-editor/layout-persistence` schema v2 | PR-0508 |
| `core-editor/editor-resolver` 多模式稳定性 | PR-0502, PR-0508 |
| `core-workspace/workspace-tree-service` 大规模一致性 | PR-0501, PR-0506 |

## 6. Release Gate

```bash
cd crates/
cargo fmt --all -- --check
cargo clippy --all -- -D warnings
cargo test --all

cd apps/lazynote_flutter/
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
```
