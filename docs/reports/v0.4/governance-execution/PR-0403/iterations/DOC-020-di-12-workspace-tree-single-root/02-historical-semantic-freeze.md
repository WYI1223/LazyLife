# DOC-020 / 02 Historical Semantic Freeze

## Purpose and Boundary

Freeze what `DI-12` actually closed before later replay documents reinterpret or partially replace it.

This stage must not:

1. treat the single-root plus fixed-system-node plan as already-landed current repo behavior;
2. ignore that `DI-12` explicitly declares itself the conceptual-parent document for a later execution chain;
3. flatten `Q1-Q12` and `E1-E6` into generic planning noise.

## Frozen Source Reading

1. `DI-12` is header-marked `RESOLVED` and explicitly declares itself the conceptual-parent document for the workspace-topology line.
2. The document closes one coherent answer set: hidden `ROOT`, real `Inbox` / `Tasks` / `Calendar` system nodes, structure-first creation routing, same-source Tasks and Calendar subtree semantics, strict active visibility, compatibility-first FFI behavior, one-shot migration, and protected delete semantics.
3. The same document explicitly hands execution detail and downstream closure to `DI-15`, `DI-16`, `DI-17`, and `DI-18`.
4. Current repo behavior and current architecture docs have not yet landed the single-root plus system-node model as present-tense normative reality.

## Freeze Result

Replay must preserve:

1. one conceptual-parent and discussion-boundary frame bundle (`DN-319-DN-321`);
2. one resolved-but-unlanded single-root workspace-topology parent bundle (`DN-322-DN-340`);
3. the fact that later replay, especially `DOC-023` through `DOC-026`, is expected to consume or revise this parent bundle rather than inherit it silently.

## References

- [`../../../PR-0401/dn-ledger.md`](../../../PR-0401/dn-ledger.md)
- [`../../../PR-0401/surveys/DOC-020-survey.md`](../../../PR-0401/surveys/DOC-020-survey.md)
- [`../../../../../v0.3/design-discussions/DI-12-workspace-tree-single-root.md`](../../../../../v0.3/design-discussions/DI-12-workspace-tree-single-root.md)
