# DOC-022 / 02 Historical Semantic Freeze

## Purpose and Boundary

Freeze `DI-14` before later workspace-topology, service, and thin-client documents reinterpret its role.

This stage must not:

1. flatten conceptual-parent framing into fake current rule text;
2. treat `Q3-Q5` as unresolved leftovers inside `DI-14`;
3. let later `DI-15-DI-17` direction erase `DI-14`'s local `Q0-Q2` closure.

## Frozen Source Facts

1. `DI-14` is header-level `PENDING`, but it explicitly marks `Q0-Q2` as `RESOLVED`.
2. `DI-14` declares itself the conceptual-parent document for Flutter-side workspace-tree core promotion.
3. `DI-14` keeps local replay-eligible closure only in `Q0-Q2`: promote shared workspace-tree infrastructure into `lib/core/workspace/`, define the shared service capability split, and define subtree-query interface shape.
4. `Q3-Q5` are explicitly migrated to `DI-17`; replay must preserve that migration boundary instead of pretending `DI-14` locally closes change notification, shared tree UI, or system-node-resolution ownership.
5. `DI-12` remains upstream conceptual input, but `DOC-020` already preserved that single-root parent bundle as accepted-but-unlanded rather than current publication.

## Frozen DN Surface

| DN Range | Freeze Role | Replay Meaning |
|------|------|------|
| `DN-347-DN-349` | conceptual-parent framing + scope boundary | explicit trace and boundary control only |
| `DN-350-DN-359` | local resolved clauses | eligible for replay into an existing published line if the stable why-question matches |
| `DN-360-DN-362` | migrated follow-up boundary | carry forward to `DI-17`, not local publication |

## Freeze Result

`DOC-022` enters replay as:

1. one conceptual-parent framing bundle;
2. one replay-eligible local closure bundle for `Q0-Q2`;
3. one explicit `DI-17` migration-boundary bundle.
